#include <iostream>

#include "../../../include/client/Views/chat_view.hh"

std::vector<Message> ChatView::get_messages(const std::vector<Message>& messages) {
    // ajout requête chargement de la classe message
    return messages;
}
