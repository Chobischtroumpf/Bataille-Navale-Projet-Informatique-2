#include "chat_view.hh"

std::vector<Message> ChatView::getMessages(const std::vector<Message>& messages) {
    // ajout requête chargement de la classe message
    return messages_;
}

void ChatView::update(){
    int test;
}